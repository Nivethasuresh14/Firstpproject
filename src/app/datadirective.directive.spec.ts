import { DatadirectiveDirective } from './datadirective.directive';

describe('DatadirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DatadirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
